# Core
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
nltk>=3.8.1
regex>=2023.10.3

# Optional: KenLM (required only for H3 entropy calculation)
# Needs cmake and libboost — see README for install instructions
# git+https://github.com/kpu/kenlm.git
